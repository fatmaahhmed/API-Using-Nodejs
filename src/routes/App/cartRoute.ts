// import express, { NextFunction, Request, Response } from "express";

// import { getCart } from "../../controllers/App/cartController";
// export const cart = express.Router();

// cart.get("/", getCart);

// cart.post("/", addToCart);

// cart.put("/:product_id", updateProductQuantity);

// cart.delete("/:product_id", removeProductFromCart);
